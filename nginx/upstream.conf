# Active environment: current (legacy)
# This will be updated to blue/green on first blue-green deploy
# For now, points to the current single-instance containers

upstream backend_active {
    server jawab24-backend:3000;
}

upstream frontend_active {
    server jawab24-frontend:3001;
}

upstream ai_worker_active {
    server jawab24-ai-worker:3002;
}

