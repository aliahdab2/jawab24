<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 
        Edge-to-Edge for Android 10+ (API 29+)
        This enables the app to draw under system bars (status bar, navigation bar)
        Required for safe area insets to be reported correctly
    -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Status bar: transparent (edge-to-edge for notch/status bar) -->
        <!-- Navigation bar: opaque (env(safe-area-inset-bottom) is unreliable in WebView) -->
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Status bar: transparent (edge-to-edge for notch/status bar) -->
        <!-- Navigation bar: opaque (env(safe-area-inset-bottom) is unreliable in WebView) -->
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>
